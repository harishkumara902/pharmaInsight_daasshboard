services:
  - type: web
    name: pharmainsight
    env: python
    runtime: python-3.11
    buildCommand: pip install -r requirements.txt && python generate_data.py
    startCommand: gunicorn app:app